[project]
name = "rag-scaffold"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.264.0",
    "constructs>=10.8.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
